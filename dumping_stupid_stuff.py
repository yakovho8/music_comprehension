# idk_img = Image.fromarray(self.ero_img)
# idk_img.save('turkish1_ero.png')
# self.gx_music = ndimage.sobel(music_sheet, axis=0, mode='constant')
# self.gy_music = ndimage.sobel(music_sheet, axis=1, mode='constant')
# Kx = -1 * np.array([[-1, 0, 1]])
# self.gx_music = ndimage.convolve(self.ero_img, Kx)

# -Derivative y
# Ky = -1 * np.array([[-1], [0], [1]])
# self.gy_music = ndimage.convolve(self.ero_img, Ky)
# self.gtheta_music = np.array([[np.arctan2(self.gy_music[i,j], self.gx_music[i,j])
#                               for j in range(len(self.gx_music[0]))]for i in range(len(self.gx_music))])
# self.gmag_music = np.hypot(self.gx_music, self.gy_music)

# self.gmag_music = self.gmag_music*(255.0/np.max(self.gmag_music))
# self.gtheta_music = self.gtheta_music*(255.0/np.max(self.gtheta_music))
# self.gtheta_music = self.gtheta_music.astype(np.uint8)
# self.gx_music.astype(int)
# self.gy_music = ndimage.gaussian_filter(self.gy_music, sigma=(0.1, 0.1), order=0)
# self.gmag_music = self.gmag_music.astype(np.uint8)
# self.gmag_music = ndimage.binary_closing(self.gmag_music)
# idk_img = Image.fromarray(self.gy_music)
# idk_img.save('turkish1_mag.png')
# idk_img = Image.fromarray(self.gx_music)
# idk_img.save('turkish1_theta.png')
# new_img = Image.fromarray(smoothed)
# stave_less_image = ndimage.gaussian_filter(stave_less_image, sigma=(0.1,0.1), order=0)
# stave_less_image = ndimage.binary_closing(stave_less_image)
# stave_less_image = ndimage.morphological_gradient(self.music_sheet, size=(3, 3))
# stave_less_image = ndimage.morphological_gradient(stave_less_image, size=(3, 3))
# stave_less_image = ndimage.binary_erosion(stave_less_image, structure=np.ones((2,2)))

# stave_less_image = ndimage.binary_opening(stave_less_image,np.ones((3,3)))

# stave_less_image = ndimage.binary_closing(stave_less_image)
# stave_less_image = ndimage.binary_closing(stave_less_image)